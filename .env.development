# 无本地后端时可保持 true；接入 API 后改为 false（或删除本行）
VITE_SKIP_API=false

# PWA「安装到桌面」弹窗：需要时改为 true（改后需重启 dev）
VITE_PWA_INSTALL_PROMPT=true

# 不设 VITE_API_BASE_URL 时，axios 走同源 `/api`，由 Vite 代理到下面地址
# 直连时取消下行注释并填写：VITE_API_BASE_URL=https://example.com/api
VITE_PROXY_TARGET=http://43.128.30.50:8080
